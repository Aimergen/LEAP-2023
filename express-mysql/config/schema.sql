create table todoCategory (
    id int unsigned auto_increment,
    name varchar(255),
    primary key (id),
);

--todo

create table todo (
    id int unsigned auto_increment,
    name varchar (266
    )
)                                              