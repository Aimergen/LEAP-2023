import React from "react";

export default function TOAST_CONFIG(){
    return (
        {
            position:'bottom-right',
            autoClose: 5000,
            hideProgressBar: false,
            theme: 'colored',
        }
    )
}